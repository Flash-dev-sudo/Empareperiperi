{
  "name": "emparo-peri-peri",
  "version": "1.0.0",
  "main": "deploy-server.js",
  "scripts": {
    "build": "npx vite build --config simple-vite.config.js && npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "node deploy-server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}